---
// このコンポーネントはクライアントサイドでログイン状態を管理します
---

<!-- ログイン前の表示 -->
<a href="/login" id="logged-out-nav" class="auth-link">ログイン</a>

<!-- ログイン後の表示 -->
<a href="/mypage" id="logged-in-nav" class="auth-link" style="display: none;">マイページ</a>

<style>
  .auth-link {
    display: block;
    padding: 16px 32px;
    color: var(--tobiracast-primary-orange);
    text-decoration: none;
    border-radius: 16px;
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    font-weight: 700;
    font-size: 1.1rem;
    border: var(--tobiracast-nav-button-border);
    backdrop-filter: blur(10px);
    background: var(--tobiracast-nav-button-bg);
    box-shadow: var(--tobiracast-nav-button-shadow);
    text-shadow: none;
    min-width: 140px;
    text-align: center;
  }

  .auth-link:hover {
    background: var(--tobiracast-nav-button-bg-hover);
    border: var(--tobiracast-nav-button-border-hover);
    color: var(--tobiracast-primary-orange);
    transform: translateY(-2px) scale(1.02);
    box-shadow: var(--tobiracast-nav-button-shadow-hover);
  }

  .auth-link:focus {
    outline: 2px solid var(--tobiracast-primary-orange);
    outline-offset: 2px;
  }

  @media (max-width: 768px) {
    .auth-link {
      padding: 8px 16px;
      width: 100%;
      text-align: center;
      min-height: auto;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.85rem;
      color: var(--tobiracast-primary-orange);
      background: transparent;
      border: 2px solid transparent;
      box-shadow: none;
      border-radius: 8px;
    }

    .auth-link:hover {
      background: rgba(255, 111, 0, 0.1);
      border-color: var(--tobiracast-primary-orange);
      transform: none;
      box-shadow: none;
    }
  }
</style>

<script>
  import { onAuthStateChanged } from 'firebase/auth'
  import { getFirebaseAuth } from '../lib/firebase-client'
  import { getNativeApiBaseUrl } from '../lib/functions-url'

  const auth = getFirebaseAuth()
  const API_URL = getNativeApiBaseUrl(import.meta.env.PUBLIC_API_URL)

  // DOM要素
  const loggedOutNav = document.getElementById('logged-out-nav')
  const loggedInNav = document.getElementById('logged-in-nav')

  // 認証状態の監視
  onAuthStateChanged(auth, async (user) => {
    if (user) {
      // アカウントが完成しているか確認
      try {
        const idToken = await user.getIdToken()
        const response = await fetch(`${API_URL}/my/profile`, {
          method: 'GET',
          headers: {
            'Authorization': idToken,
            'Content-Type': 'application/json'
          }
        })
        const data = await response.json()

        if (data.status === 'success') {
          loggedOutNav.style.display = 'none'
          loggedInNav.style.display = 'block'
          return
        }
      } catch {
        // API呼び出し失敗時はログインリンク表示
      }

      // アカウント未完成 → ログイン表示のまま
      loggedOutNav.style.display = 'block'
      loggedInNav.style.display = 'none'
    } else {
      // 未ログイン
      loggedOutNav.style.display = 'block'
      loggedInNav.style.display = 'none'
    }
  })
</script>
