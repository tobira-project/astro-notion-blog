---
import Layout from '../layouts/Layout.astro'
---

<Layout title="加入キャンセル" path="/cancel">
  <div slot="main" class="cancel-page">
    <div class="cancel-container">
      <div class="cancel-icon">×</div>
      <h1 class="cancel-title">プランへの加入がキャンセルされました</h1>
      <p class="cancel-message">
        お支払いは完了していません。<br />
        プランに加入する場合は、再度お手続きをお願いします。
      </p>
      <div class="action-buttons">
        <a href="/subscription" class="button primary-button">プラン選択に戻る</a>
        <a href="/" class="button secondary-button">トップページへ</a>
      </div>
    </div>
  </div>
</Layout>

<style>
  .cancel-page {
    width: 100%;
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 1rem;
  }

  .cancel-container {
    max-width: 600px;
    text-align: center;
    background: white;
    border-radius: 16px;
    padding: 3rem 2rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  }

  .cancel-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 2rem;
    background: linear-gradient(135deg, #f59e0b, #d97706);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 3.5rem;
    font-weight: bold;
    line-height: 1;
    box-shadow: 0 4px 20px rgba(245, 158, 11, 0.3);
  }

  .cancel-title {
    font-size: 2rem;
    color: #111827;
    margin-bottom: 1rem;
  }

  .cancel-message {
    font-size: 1.1rem;
    color: #6b7280;
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .action-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .button {
    padding: 0.875rem 2rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.3s;
    display: inline-block;
  }

  .primary-button {
    background: linear-gradient(135deg, var(--tobiracast-primary-blue), var(--tobiracast-light-blue));
    color: white;
    box-shadow: 0 4px 12px rgba(23, 121, 222, 0.3);
  }

  .primary-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(23, 121, 222, 0.4);
  }

  .secondary-button {
    background: white;
    color: var(--tobiracast-primary-blue);
    border: 2px solid var(--tobiracast-primary-blue);
  }

  .secondary-button:hover {
    background: var(--tobiracast-primary-blue);
    color: white;
  }

  @media (max-width: 768px) {
    .cancel-container {
      padding: 2rem 1.5rem;
    }

    .cancel-title {
      font-size: 1.5rem;
    }

    .action-buttons {
      flex-direction: column;
    }

    .button {
      width: 100%;
    }
  }
</style>
