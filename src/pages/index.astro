---
import {
  getPosts,
} from '../lib/notion/client.ts'
import { getNavLink } from '../lib/blog-helpers.ts'
import Layout from '../layouts/Layout.astro'
import NoContents from '../components/NoContents.astro'
import PostDate from '../components/PostDate.astro'
import PostFeaturedImage from '../components/PostFeaturedImage.astro'
import styles from '../styles/blog.module.css'

const [posts] = await Promise.all([
  getPosts(6), // 最新6記事のみ取得
])
---

<Layout>
  <div slot="main" class={styles.main}>
    <section class="articles-section">
      <div class="section-header">
        <h2 class="section-title">最新記事</h2>
        <p class="section-subtitle">Tobiratoryから最新のインサイトをお届けします</p>
      </div>
      {
        posts.length === 0 ? (
          <NoContents contents={posts} />
        ) : (
          <>
            <div class="latest-articles">
              {posts.map((post, index) => (
                <a href={getNavLink(`/posts/${post.Slug}`)} class="article-card-link">
                  <article class="article-card" key={post.Slug} style={`animation-delay: ${index * 0.1}s`}>
                    <div class="article-card-image">
                      <PostFeaturedImage post={post} />
                      <div class="article-card-overlay">
                        <span class="read-more-text">続きを読む</span>
                      </div>
                    </div>
                    <div class="article-card-content">
                      <h3 class="article-card-title">{post.Title}</h3>
                      <PostDate post={post} />
                    </div>
                  </article>
                </a>
              ))}
            </div>
            <div class="view-all-articles">
              <a href={getNavLink('/posts')} class="view-all-articles-link">
                <span>記事一覧へ</span>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            </div>
          </>
        )
      }
    </section>
  </div>
</Layout>

<style>
.articles-section {
  max-width: 1200px;
  margin: 0 auto;
}

.section-header {
  text-align: center;
  margin-bottom: 3rem;
  animation: fadeIn 0.4s ease-out;
}

.section-title {
  color: var(--tobiracast-primary-blue);
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  position: relative;
  display: inline-block;
}

.section-title::after {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 50%;
  transform: translateX(-50%);
  width: 60px;
  height: 4px;
  background: linear-gradient(90deg, var(--tobiracast-primary-blue), var(--tobiracast-primary-orange));
  border-radius: 2px;
}

.section-subtitle {
  color: var(--tobiracast-text-secondary);
  font-size: 1.1rem;
  margin: 0;
  opacity: 0.8;
}

.latest-articles {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
  gap: 2rem;
  margin: 2rem 0 4rem;
}

.article-card {
  background: var(--tobiracast-bg-primary);
  border-radius: 16px;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: var(--tobiracast-card-shadow);
  position: relative;
  animation: fadeIn 0.4s ease-out;
}

.article-card:hover {
  transform: translateY(-8px) scale(1.02);
  box-shadow: var(--tobiracast-card-shadow-hover);
}

.article-card-image {
  position: relative;
  overflow: hidden;
}

.article-card-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--tobiracast-overlay-gradient);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.article-card:hover .article-card-overlay {
  opacity: 1;
}

.read-more-text {
  color: white;
  font-size: 1.1rem;
  font-weight: 600;
  text-shadow: var(--tobiracast-text-shadow-medium);
}

.article-card-content {
  padding: 1.5rem;
}

.article-card-title {
  color: var(--tobiracast-text-primary);
  font-size: 1.2rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
  line-height: 1.4;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.article-card-link {
  text-decoration: none;
  color: inherit;
  display: block;
}

.article-card-link:hover {
  color: inherit;
}

.view-all-articles {
  text-align: center;
  margin: 4rem 0;
}

.view-all-articles-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 2.5rem;
  background: linear-gradient(135deg, var(--tobiracast-primary-orange), var(--tobiracast-dark-orange));
  color: white;
  border-radius: 50px;
  font-weight: 600;
  font-size: 1.1rem;
  text-decoration: none;
  transition: all 0.3s ease;
  box-shadow: var(--tobiracast-button-shadow-orange-strong);
}

.view-all-articles-link:hover {
  transform: translateY(-3px);
  box-shadow: var(--tobiracast-button-shadow-orange-strong-hover);
  color: white;
}

.view-all-articles-link svg {
  transition: transform 0.3s ease;
}

.view-all-articles-link:hover svg {
  transform: translateX(5px);
}

/* Override PostFeaturedImage styles for grid layout */
.article-card-image :global(.featured-image) {
  margin: 0;
  width: 100%;
  height: 220px;
}

.article-card-image :global(.featured-image img) {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s ease;
}

.article-card:hover .article-card-image :global(.featured-image img) {
  transform: scale(1.05);
}

/* Override PostDate styles for card layout */
.article-card-content :global(.post-date) {
  font-size: 0.9rem;
  color: var(--tobiracast-text-muted);
  margin: 0;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.article-card-content :global(.post-date)::before {
  content: '📅';
  font-size: 0.8rem;
}

/* Responsive Design */
@media (max-width: 768px) {
  .section-title {
    font-size: 2rem;
  }

  .section-subtitle {
    font-size: 1rem;
  }

  .latest-articles {
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin: 1.5rem 0 3rem;
  }

  .article-card-content {
    padding: 1.25rem;
  }

  .article-card-title {
    font-size: 1.1rem;
  }

  .view-all-articles {
    margin: 3rem 0;
  }

  .view-all-articles-link {
    padding: 0.875rem 2rem;
    font-size: 1rem;
  }
}

@media (max-width: 480px) {
  .section-title {
    font-size: 1.75rem;
  }

  .section-subtitle {
    font-size: 0.95rem;
  }

  .latest-articles {
    grid-template-columns: 1fr;
    gap: 1rem;
    margin: 1rem 0 2rem;
  }

  .article-card-content {
    padding: 1rem;
  }

  .article-card-title {
    font-size: 1rem;
  }

  .article-card-image :global(.featured-image) {
    height: 180px;
  }

  .view-all-articles-link {
    padding: 0.75rem 1.5rem;
    font-size: 0.95rem;
  }
}
</style>
