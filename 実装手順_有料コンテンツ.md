# 有料コンテンツ実装手順

## 📋 実装の流れ

### Step 1: コンポーネント統合（今すぐできる）

1. **PremiumContent.astroを記事ページに組み込む**

```astro
// src/pages/posts/[slug].astro に追加
---
import PremiumContent from '../../components/PremiumContent.astro'

// ユーザーの有料会員状態を取得（仮実装）
const isUserPremium = false // TODO: 実際の判定ロジックを実装
---

<!-- 記事本文の表示部分を置き換え -->
<PremiumContent
  content={post.content}
  isUserPremium={isUserPremium}
  postSlug={post.slug}
/>
```

### Step 2: 有料会員判定（Firebase Auth連携）

```typescript
// src/lib/auth-helper.ts
import { getAuth } from 'firebase/auth';

export async function checkPremiumStatus(): Promise<boolean> {
  const auth = getAuth();
  const user = auth.currentUser;

  if (!user) return false;

  try {
    // IDトークンを取得
    const idToken = await user.getIdToken();

    // APIを呼んで有料会員状態を確認
    const response = await fetch('/api/check-premium', {
      headers: {
        Authorization: `Bearer ${idToken}`,
      },
    });

    const data = await response.json();
    return data.isPremium;
  } catch (error) {
    console.error('Premium check failed:', error);
    return false;
  }
}
```

### Step 3: サブスクリプションページ作成

```astro
// src/pages/subscription.astro
---
// 既存のページを拡張
---

<div class="subscription-plans">
  <div class="plan basic">
    <h3>ベーシック</h3>
    <div class="price">¥980/月</div>
    <ul>
      <li>すべての有料記事</li>
      <li>月10トークン</li>
    </ul>
    <button class="select-plan" data-price-id="price_basic">
      このプランを選択
    </button>
  </div>

  <div class="plan premium">
    <h3>プレミアム</h3>
    <div class="price">¥1,980/月</div>
    <ul>
      <li>すべての有料記事</li>
      <li>月30トークン</li>
      <li>限定コンテンツ</li>
    </ul>
    <button class="select-plan" data-price-id="price_premium">
      このプランを選択
    </button>
  </div>
</div>

<script>
// Stripe Checkout への遷移
document.querySelectorAll('.select-plan').forEach(button => {
  button.addEventListener('click', async (e) => {
    const priceId = e.target.dataset.priceId

    // Stripe Checkout セッション作成API呼び出し
    const response = await fetch('/api/create-checkout-session', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ priceId })
    })

    const { url } = await response.json()
    window.location.href = url // Stripeの決済ページへ
  })
})
</script>
```

### Step 4: API エンドポイント作成

```typescript
// src/pages/api/check-premium.ts
export async function GET({ request }) {
  const token = request.headers.get('Authorization')?.replace('Bearer ', '');

  if (!token) {
    return new Response(JSON.stringify({ isPremium: false }), {
      status: 401,
    });
  }

  // TODO: Firebaseトークン検証
  // TODO: blog_accountsテーブル照会

  return new Response(
    JSON.stringify({
      isPremium: true, // 仮の値
    }),
    {
      headers: { 'Content-Type': 'application/json' },
    }
  );
}
```

## 🎯 今すぐテストできること

### 1. 静的なペイウォール表示

```astro
// テスト用: src/pages/test-paywall.astro
---
import PremiumContent from '../components/PremiumContent.astro'

const testContent = `
<h2>無料で読める部分</h2>
<p>この記事では新機能について説明します。</p>
<p>まず基本的な使い方から...</p>

<hr class="divider">

<h2>有料会員限定</h2>
<p>ここから詳細な実装方法を解説します。</p>
<p>高度なテクニックも紹介...</p>
`
---

<h1>ペイウォールテスト</h1>

<!-- 無料ユーザーとして表示 -->
<h3>無料ユーザーの見え方</h3>
<PremiumContent
  content={testContent}
  isUserPremium={false}
  postSlug="test"
/>

<!-- 有料会員として表示 -->
<h3>有料会員の見え方</h3>
<PremiumContent
  content={testContent}
  isUserPremium={true}
  postSlug="test"
/>
```

### 2. Notionで実際の記事作成

1. Notionで記事を書く
2. 無料部分を書く
3. **区切り線を挿入**（/divider）
4. 有料部分を書く
5. Publishして確認

## ⚡ クイックスタート

```bash
# 1. テストページを作成
npm run dev

# 2. http://localhost:4321/test-paywall にアクセス

# 3. ペイウォールの表示を確認
```

## 📊 実装優先順位

1. **今すぐ** - PremiumContent.astroの動作確認
2. **Stripe情報取得後** - 決済フロー実装
3. **API準備後** - blog_accountsとの連携
4. **最後** - 管理画面、分析機能

---

これで有料コンテンツ機能の基本実装は完了です！
